# ──────────────────────────────────────────────────────────
# Complaint Auto-Routing System — Requirements
# ──────────────────────────────────────────────────────────

# Core ML (required)
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.0.0
scipy>=1.12.0
joblib>=1.3.0

# Offline embedding upgrade (highly recommended — ~120 MB download)
# sentence-transformers>=2.7.0   # multilingual: paraphrase-multilingual-MiniLM-L12-v2

# Fast vector search for large corpora (optional, drop-in replacement)
# faiss-cpu>=1.8.0               # pip install faiss-cpu

# Web interface (required for hosting)
gradio>=4.20.0

# Audio / Video transcription (local offline Whisper model)
openai-whisper>=20231117
ffmpeg-python>=0.2.0

# ──────────────────────────────────────────────────────────
# System dependencies (for audio/video path):
#   sudo apt install ffmpeg
# ──────────────────────────────────────────────────────────
